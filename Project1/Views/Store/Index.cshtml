@model IEnumerable<Project1.ViewModels.Store>

@{
    ViewData["Title"] = "Index";
}

<h1>Store Locations</h1>

<p>
    <h4>Select your store location</h4>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.PhoneNumber)
            </th>*@
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.ActionLink(item.Location, "Details", new { id = item.StoreId })
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.StoreId }) |
                @Html.ActionLink("Details", "Details", new { id = item.StoreId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.StoreId })
            </td>*@
        </tr>
}
    </tbody>
</table>
<div>
    @Html.ActionLink("Back to Customer Selection", "LoginCustomer")
</div>
